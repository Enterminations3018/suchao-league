name: Deploy to GitHub Pages

on:
  push:
    branches:
      - master
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore dependencies
        run: dotnet restore suchao-wasm/SuchaoLeagueWasm.csproj

      - name: Build
        run: dotnet build suchao-wasm/SuchaoLeagueWasm.csproj --configuration Release --no-restore

      - name: Publish
        run: dotnet publish suchao-wasm/SuchaoLeagueWasm.csproj --configuration Release --no-build --output ./publish

      - name: Fix blazor.webassembly.js reference
        run: |
          cd ./publish/wwwroot/_framework
          WASM_FILE=$(ls blazor.webassembly.*.js | head -n 1)
          if [ -n "$WASM_FILE" ]; then
            cp "$WASM_FILE" blazor.webassembly.js
          fi

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './publish/wwwroot'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
